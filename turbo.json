{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "inputs": ["src/**/*.ts", "tsup.config.js"],
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    }
  }
}
